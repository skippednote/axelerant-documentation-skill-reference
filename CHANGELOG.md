# Changelog

## 1.0.0-rc.1 — September 3, 2026

Candidate, not deployed. Adds validated configuration, scoped exemptions, evidence methods, ownership and agent checks, rule coverage tests, a local executable notification example, consistent CI enforcement and a release gate. Existing sample ADRs are superseded by 0003, not deleted.

Browser installation, the refreshed npm lockfile and live Claude plugin loading remain release requirements.
