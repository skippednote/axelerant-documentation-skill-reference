#!/usr/bin/env bash
# Compatibility stub. The trusted reusable workflow now uses the GitHub API
# directly and never executes pull-request repository scripts with a write token.
printf '%s\n' 'Documentation reminder is implemented by docs-coupling.yml.'
exit 0
